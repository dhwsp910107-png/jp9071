# 타이머 중복 생성 문제 해결

## 문제
- .md 파일을 열 때마다 타이머가 여러 개 생성됨
- 페이지 새로고침 시 타이머가 계속 추가됨

## 해결 방법

### 1. initializeTimer 함수에서 로깅 추가
기존:
```javascript
console.log('⏰ 새 타이머 생성됨');
```

변경:
```javascript
console.log('⏰ 새 타이머 생성됨 - 총', this.activeTimers.length, '개');
```

### 2. cleanupTimers 함수 개선
기존:
```javascript
cleanupTimers() {
    if (this.activeTimers) {
        this.activeTimers.forEach(timer => timer.destroy());
        this.activeTimers = [];
    }
}
```

변경:
```javascript
cleanupTimers() {
    if (this.activeTimers) {
        console.log('🗑️ 타이머 정리 중:', this.activeTimers.length, '개');
        this.activeTimers.forEach(timer => timer.destroy());
        this.activeTimers = [];
    }
}
```

### 3. processClozes에서 타이머 생성 로직 개선
- 현재 활성 뷰의 컨테이너에서만 타이머 생성
- 이미 타이머가 있으면 건너뜀
- 로그로 타이머 생성 추적

### 4. 페이지 전환 시 타이머 정리
- refreshAllClozes() 시작 시 기존 타이머 정리
- 새로운 페이지 로드 시 이전 타이머 제거

## 테스트 방법
1. 빈칸 노트 열기
2. 콘솔에서 "⏰ 새 타이머 생성됨" 메시지 확인
3. 다른 노트로 이동
4. 다시 빈칸 노트로 돌아오기
5. 타이머가 1개만 있는지 확인

## 진동 에러 해결
- navigator.vibrate()를 try-catch로 감싸기
- 사용자 인터랙션 없이는 진동이 작동하지 않음 (브라우저 정책)
- 에러를 무시하고 계속 진행
