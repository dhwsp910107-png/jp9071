# 📚 Study Dashboard Plugin for Obsidian

> 체계적인 학습 관리를 위한 폴더별 문제 은행 대시보드

![Plugin Version](https://img.shields.io/badge/version-1.0.0-blue)
![Obsidian Compatibility](https://img.shields.io/badge/obsidian-0.15.0%2B-purple)
![License](https://img.shields.io/badge/license-MIT-green)

## 🌟 주요 기능

- **📊 시각적 대시보드**: 과목별 학습 진도를 한눈에 파악
- **📝 문제 은행 관리**: 번호별 문제 생성 및 상태 추적
- **📈 진행률 추적**: 실시간 학습 통계 및 목표 달성률
- **🎯 과목별 분류**: 수학, 물리, 화학 등 다양한 과목 지원
- **⚡ 빠른 문제 생성**: 클릭 한 번으로 새 문제 파일 생성
- **🎨 모던 UI**: 그라디언트와 애니메이션이 적용된 세련된 디자인

## 📦 설치 방법

### 방법 1: 수동 설치 (권장)

1. **플러그인 폴더로 이동**
   ```
   .obsidian/plugins/ 폴더 이동
   ```

2. **플러그인 폴더 생성**
   ```
   study-dashboard/ 폴더 생성
   ```

3. **파일 복사**
   - `main.js` 파일 복사
   - `manifest.json` 파일 복사

4. **플러그인 활성화**
   - Obsidian 설정 → Community plugins
   - "Study Dashboard" 찾아서 활성화

### 방법 2: 개발자 모드

1. **개발자 콘솔에서 설치**
   ```bash
   cd .obsidian/plugins
   git clone [repository-url] study-dashboard
   cd study-dashboard
   npm install
   ```

## 🚀 빠른 시작

### 1단계: 플러그인 활성화
- Obsidian 설정 → Community plugins → Study Dashboard ✅

### 2단계: 초기 설정
1. 설정에서 **"필수 폴더 생성"** 버튼 클릭
2. 원하는 과목 목록 설정 (기본: 수학, 물리, 화학, 생물, 영어, 국어, 한국사)

### 3단계: 대시보드 열기
- 🔗 리본 아이콘(📊) 클릭
- 또는 명령어 팔레트: `Ctrl/Cmd + P` → "학습 대시보드 열기"

### 4단계: 첫 문제 만들기
1. **새 문제 만들기** 버튼 클릭
2. 문제 정보 입력:
   - 번호: 1
   - 제목: "이차방정식 풀이"
   - 과목: 수학
   - 난이도: ⭐⭐⭐
3. **문제 만들기** 클릭

## ⚙️ 상세 설정

### 📁 폴더 구조 설정

```
학습관리/
├── 문제은행/          # 문제 파일들이 저장되는 폴더
│   ├── 001_이차방정식.md
│   ├── 002_미분공식.md
│   └── ...
└── 템플릿/            # 문제 템플릿 폴더
    └── 문제-템플릿.md
```

### 🎛️ 플러그인 설정

**설정 → Community plugins → Study Dashboard → ⚙️**

| 설정 항목 | 설명 | 기본값 |
|-----------|------|--------|
| **문제 폴더 경로** | 문제 파일들이 저장될 폴더 | `학습관리/문제은행` |
| **템플릿 폴더 경로** | 템플릿 파일들이 저장된 폴더 | `학습관리/템플릿` |
| **일일 목표** | 하루에 풀고자 하는 문제 수 | `5문제` |
| **목표 완료일** | 모든 문제를 완료하고자 하는 날짜 | `2025-12-31` |
| **자동 백업** | 문제 상태 변경 시 자동 백업 | `활성화` |
| **과목 목록** | 학습할 과목들 (쉼표로 구분) | `수학, 물리, 화학...` |

### 📝 문제 파일 형식

생성되는 문제 파일은 다음과 같은 구조를 가집니다:

```markdown
---
number: 1
title: "이차방정식 풀이"
subject: "수학"
status: "unknown"
difficulty: 3
created: 2025-09-23
updated: 2025-09-23
tags: [수학, 문제]
---

# 1. 이차방정식 풀이

## 문제
x² - 5x + 6 = 0을 풀어라.

## 풀이
(x - 2)(x - 3) = 0
따라서 x = 2 또는 x = 3

## 답
x = 2, 3

## 노트
- 인수분해를 이용한 풀이
- 관련 문제: [[003_이차공식]]
```

### 🎯 문제 상태 관리

| 상태 | 색상 | 의미 |
|------|------|------|
| **unknown** | 🟣 보라 | 아직 풀지 않은 문제 |
| **learning** | 🔴 빨강 | 학습 중인 문제 |
| **reviewing** | 🟡 노랑 | 복습이 필요한 문제 |
| **mastered** | 🟢 초록 | 완전히 숙달한 문제 |

## 🎨 CSS 커스터마이징

### 색상 테마 변경

CSS 스니펫을 생성하여 색상을 커스터마이징할 수 있습니다:

```css
/* 설정 → Appearance → CSS snippets → 새 스니펫 생성 */

/* 메인 테마 색상 변경 */
.study-dashboard-view .study-dashboard-header {
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%) !important;
}

/* 문제 상태별 색상 커스터마이징 */
.study-dashboard-view .problem-number.mastered {
    background: linear-gradient(45deg, #11b981, #059669) !important;
}

.study-dashboard-view .problem-number.reviewing {
    background: linear-gradient(45deg, #f59e0b, #d97706) !important;
}

.study-dashboard-view .problem-number.learning {
    background: linear-gradient(45deg, #ef4444, #dc2626) !important;
}

.study-dashboard-view .problem-number.unknown {
    background: linear-gradient(45deg, #8b5cf6, #7c3aed) !important;
}

/* 다크모드 최적화 */
.theme-dark .study-dashboard-view .stat-card {
    background: var(--background-secondary) !important;
    border: 1px solid var(--background-modifier-border) !important;
}
```

### 애니메이션 비활성화

모션을 줄이고 싶다면:

```css
/* 애니메이션 비활성화 CSS 스니펫 */
.study-dashboard-view * {
    animation: none !important;
    transition: none !important;
}
```

## 🔧 문제 해결

### 일반적인 문제들

#### 1. **플러그인이 로드되지 않음**
```
해결책:
1. Obsidian 재시작
2. Community plugins에서 비활성화 후 재활성화
3. 플러그인 폴더 확인: .obsidian/plugins/study-dashboard/
```

#### 2. **문제 파일이 생성되지 않음**
```
해결책:
1. 설정에서 "필수 폴더 생성" 클릭
2. 폴더 경로 확인 (기본: 학습관리/문제은행)
3. 쓰기 권한 확인
```

#### 3. **CSS 스타일이 적용되지 않음**
```
해결책:
1. 다른 CSS 스니펫과 충돌 확인
2. 캐시 새로고침: Ctrl+Shift+R
3. Safe mode에서 테스트
```

#### 4. **대시보드가 비어있음**
```
해결책:
1. 문제 폴더에 .md 파일 확인
2. frontmatter 형식 확인
3. 새로고침 버튼 클릭
```

### 성능 최적화

#### 대용량 문제 은행 (500개+ 문제)
```css
/* 그리드 최적화 */
.study-dashboard-view .problems-grid {
    max-height: 600px;
    overflow-y: auto;
}

/* 애니메이션 간소화 */
.study-dashboard-view .problem-number:hover {
    transform: scale(1.05) !important; /* rotate 제거 */
}
```

## 📊 고급 사용법

### 1. 진도 추적 자동화

Templater 플러그인과 연동하여 자동 진도 업데이트:

```javascript
// Templater 스크립트
<%*
const file = tp.config.target_file;
const currentStatus = await tp.file.cursor();
// 상태 자동 업데이트 로직
%>
```

### 2. 학습 통계 내보내기

```javascript
// 콘솔에서 실행
const stats = app.plugins.plugins['study-dashboard'].getStats();
console.log(JSON.stringify(stats, null, 2));
```

### 3. 일괄 문제 가져오기

기존 노트를 문제 형식으로 변환:

```bash
# 스크립트 예시
for file in *.md; do
  # frontmatter 추가
  sed -i '1i---' "$file"
  sed -i '2inumber: 1' "$file"
  sed -i '3ititle: "제목"' "$file"
  # ... 기타 변환 작업
done
```

## 🤝 기여하기

### 버그 리포트
1. 이슈 생성시 다음 정보 포함:
   - Obsidian 버전
   - 플러그인 버전
   - 오류 메시지 (개발자 콘솔)
   - 재현 단계

### 기능 요청
- 새로운 기능 아이디어
- UI/UX 개선 제안
- 성능 최적화 아이디어

### 개발 참여
```bash
# 개발 환경 설정
git clone [repository]
cd study-dashboard
npm install
npm run dev
```

## 📄 라이선스

MIT License - 자유롭게 사용, 수정, 배포 가능

## 🙏 크레딧

- Obsidian API 팀
- 커뮤니티 플러그인 개발자들
- 베타 테스터들

---

## 📞 지원

- **문서**: 이 README
- **이슈**: GitHub Issues
- **커뮤니티**: Obsidian Discord #plugin-dev

**즐거운 학습 되세요! 📚✨**
