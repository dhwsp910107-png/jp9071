# 물품 목록표

```dataviewjs
const items = dv.pages('"Items"')
    .sort(i => i.file.name)

dv.table(
    ["물품명", "대분류", "소분류", "위치", "수량", "등록일"],
    items.map(i => [
        i.file.link,
        i.category || '-',
        i.subcategory || '-',
        i.location || '-',
        i.quantity || 1,
        i.file.ctime ? i.file.ctime.toFormat("yyyy-MM-dd") : '-'
    ])
)
```

---

## 필터링된 목록

### 대분류별

#### 📚 책
```dataviewjs
dv.table(
    ["물품명", "상태", "위치", "수량"],
    dv.pages('"Items"')
        .where(i => i.category === '책')
        .map(i => [i.file.link, i.subcategory, i.location, i.quantity])
)
```

#### 👕 옷
```dataviewjs
dv.table(
    ["물품명", "상태", "위치", "수량"],
    dv.pages('"Items"')
        .where(i => i.category === '옷')
        .map(i => [i.file.link, i.subcategory, i.location, i.quantity])
)
```

#### 🔌 전자제품
```dataviewjs
dv.table(
    ["물품명", "상태", "위치", "수량"],
    dv.pages('"Items"')
        .where(i => i.category === '전자제품')
        .map(i => [i.file.link, i.subcategory, i.location, i.quantity])
)
```

### 상태별

#### ⚠️ 버릴 물품
```dataviewjs
dv.table(
    ["물품명", "카테고리", "위치", "수량"],
    dv.pages('"Items"')
        .where(i => i.subcategory && i.subcategory.includes('버릴'))
        .map(i => [i.file.link, i.category, i.location, i.quantity])
)
```

#### ✅ 사용 중인 물품
```dataviewjs
dv.table(
    ["물품명", "카테고리", "위치", "수량"],
    dv.pages('"Items"')
        .where(i => i.subcategory && (i.subcategory.includes('사용') || i.subcategory.includes('읽는')))
        .map(i => [i.file.link, i.category, i.location, i.quantity])
)
```

#### 📦 보관 중인 물품
```dataviewjs
dv.table(
    ["물품명", "카테고리", "위치", "수량"],
    dv.pages('"Items"')
        .where(i => i.subcategory && (i.subcategory.includes('보관') || i.subcategory.includes('계절')))
        .map(i => [i.file.link, i.category, i.location, i.quantity])
)
```
