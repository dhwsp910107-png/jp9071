---
cssclasses:
  - dashboard
times: ["13:49:02"]
---

# 🚀 슈퍼허브 대시보드

> **폴더강의 학습시스템 - 차세대 인터랙티브 대시보드**

---

## 📅 주차별 학습 일정

```dataviewjs
// 날짜만 사용하도록 정규화 (시간/분/초 제거)
const now = moment().startOf('day');
const year = now.year();
const month = now.month() + 1;
const weekOfMonth = Math.ceil(now.date() / 7);
const currentWeekFile = `${year}년 ${month}월 ${weekOfMonth}주차 일정`;

const nextWeek = now.clone().add(7, 'days');
const nextYear = nextWeek.year();
const nextMonth = nextWeek.month() + 1;
const nextWeekOfMonth = Math.ceil(nextWeek.date() / 7);
const nextWeekFile = `${nextYear}년 ${nextMonth}월 ${nextWeekOfMonth}주차 일정`;

const prevWeek = now.clone().subtract(7, 'days');
const prevYear = prevWeek.year();
const prevMonth = prevWeek.month() + 1;
const prevWeekOfMonth = Math.ceil(prevWeek.date() / 7);
const prevWeekFile = `${prevYear}년 ${prevMonth}월 ${prevWeekOfMonth}주차 일정`;

const scheduleFolder = "📅 학습일정";

// HTML 렌더링 (CSS 클래스 사용)
const container = dv.container;
container.innerHTML = `
<div class="weekly-schedule-container">
  <h3>📅 ${year}년 ${month}월 ${weekOfMonth}주차</h3>
  <div class="weekly-buttons">
    <button id="prev-week-btn">◀️ 이전 주</button>
    <button id="current-week-btn" class="current-week">📋 이번 주 일정</button>
    <button id="next-week-btn">다음 주 ▶️</button>
  </div>
  <div class="weekly-edit-btn">
    <button id="edit-btn">✏️ 일정 편집하기</button>
  </div>
</div>
`;

// 이벤트 핸들러 등록
setTimeout(() => {
  const prevBtn = container.querySelector('#prev-week-btn');
  const currentBtn = container.querySelector('#current-week-btn');
  const nextBtn = container.querySelector('#next-week-btn');
  const editBtn = container.querySelector('#edit-btn');
  
  if (prevBtn) {
    prevBtn.addEventListener('click', async () => {
      const filePath = `${scheduleFolder}/${prevWeekFile}.md`;
      try {
        const file = app.vault.getAbstractFileByPath(filePath);
        if (!file) {
          await createWeeklySchedule(scheduleFolder, prevWeekFile, prevYear, prevMonth, prevWeekOfMonth);
        }
        await app.workspace.openLinkText(filePath, '', false);
      } catch (e) {
        console.error('파일 열기 오류:', e);
      }
    });
  }
  
  if (currentBtn) {
    currentBtn.addEventListener('click', async () => {
      const filePath = `${scheduleFolder}/${currentWeekFile}.md`;
      try {
        const file = app.vault.getAbstractFileByPath(filePath);
        if (!file) {
          await createWeeklySchedule(scheduleFolder, currentWeekFile, year, month, weekOfMonth);
        }
        await app.workspace.openLinkText(filePath, '', false);
      } catch (e) {
        console.error('파일 열기 오류:', e);
      }
    });
  }
  
  if (nextBtn) {
    nextBtn.addEventListener('click', async () => {
      const filePath = `${scheduleFolder}/${nextWeekFile}.md`;
      try {
        const file = app.vault.getAbstractFileByPath(filePath);
        if (!file) {
          await createWeeklySchedule(scheduleFolder, nextWeekFile, nextYear, nextMonth, nextWeekOfMonth);
        }
        await app.workspace.openLinkText(filePath, '', false);
      } catch (e) {
        console.error('파일 열기 오류:', e);
      }
    });
  }
  
  if (editBtn) {
    editBtn.addEventListener('click', async () => {
      const filePath = `${scheduleFolder}/${currentWeekFile}.md`;
      try {
        const file = app.vault.getAbstractFileByPath(filePath);
        if (!file) {
          await createWeeklySchedule(scheduleFolder, currentWeekFile, year, month, weekOfMonth);
        }
        await app.workspace.openLinkText(filePath, '', false);
      } catch (e) {
        console.error('파일 열기 오류:', e);
      }
    });
  }
}, 100);

// 주간 일정 파일 생성 함수
async function createWeeklySchedule(folder, fileName, year, month, week) {
  const weekStart = moment().year(year).month(month - 1).date((week - 1) * 7 + 1).startOf('week');
  const weekEnd = weekStart.clone().endOf('week');
  
  const content = `---
week: ${week}
month: ${month}
year: ${year}
created: ${moment().format('YYYY-MM-DD')}
---

# 📅 ${year}년 ${month}월 ${week}주차 일정

> 📆 ${weekStart.format('M월 D일')} ~ ${weekEnd.format('M월 D일')}

## 🎯 이번 주 목표

- [ ] 

## 📚 학습 계획

### 월요일 (${weekStart.clone().add(0, 'days').format('M/D')})
- [ ] 

### 화요일 (${weekStart.clone().add(1, 'days').format('M/D')})
- [ ] 

### 수요일 (${weekStart.clone().add(2, 'days').format('M/D')})
- [ ] 

### 목요일 (${weekStart.clone().add(3, 'days').format('M/D')})
- [ ] 

### 금요일 (${weekStart.clone().add(4, 'days').format('M/D')})
- [ ] 

### 주말 (${weekStart.clone().add(5, 'days').format('M/D')}-${weekStart.clone().add(6, 'days').format('M/D')})
- [ ] 

## 📊 주간 진행률

\`\`\`dataviewjs
const currentFile = dv.current();
const allTasks = currentFile.file.tasks;
const completed = allTasks.filter(t => t.completed).length;
const total = allTasks.length;
const percent = total > 0 ? Math.round((completed / total) * 100) : 0;

dv.paragraph(\`
<div style="background: linear-gradient(135deg, #10b981 0%, #059669 100%); padding: 20px; border-radius: 12px; color: white;">
  <h3 style="margin: 0 0 10px 0;">✅ 주간 달성률</h3>
  <div style="font-size: 48px; font-weight: bold;">\${percent}%</div>
  <div style="opacity: 0.9; margin-top: 10px;">완료: \${completed} / 전체: \${total}</div>
  <div style="margin-top: 15px; background: rgba(255,255,255,0.2); height: 10px; border-radius: 5px;">
    <div style="background: white; height: 100%; width: \${percent}%; border-radius: 5px; transition: width 0.5s;"></div>
  </div>
</div>
\`);
\`\`\`

---

*생성일: ${moment().format('YYYY-MM-DD')}*
`;

  try {
    const folderPath = folder;
    const folderExists = await app.vault.adapter.exists(folderPath);
    if (!folderExists) {
      await app.vault.createFolder(folderPath);
    }
    
    const filePath = `${folderPath}/${fileName}.md`;
    await app.vault.create(filePath, content);
    
    if (window.Notice) {
      new Notice(`✅ ${fileName} 생성 완료!`);
    }
  } catch (error) {
    console.error('파일 생성 오류:', error);
    if (window.Notice) {
      new Notice(`❌ 파일 생성 실패: ${error.message}`);
    }
  }
}
```

---

## ⚡ 빠른 액션 센터

```dataviewjs
const actionStyles = `
<style>
.action-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 15px;
    margin: 20px 0;
}
.action-card {
    padding: 15px;
    border-radius: 10px;
    text-align: center;
    color: white;
    cursor: pointer;
    transition: all 0.3s ease;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
}
.action-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 25px rgba(0,0,0,0.2);
}
.action-card-1 { background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); }
.action-card-2 { background: linear-gradient(135deg, #f093fb 0%, #f5576c 100%); }
.action-card-3 { background: linear-gradient(135deg, #4facfe 0%, #00f2fe 100%); }
.action-card-4 { background: linear-gradient(135deg, #43e97b 0%, #38f9d7 100%); }
.action-title { font-weight: bold; font-size: 1rem; margin-bottom: 8px; }
.action-desc { font-size: 0.8rem; opacity: 0.9; }
</style>
`;

dv.paragraph(actionStyles);

const actionsHTML = `
<div class="action-grid">
    <div class="action-card action-card-1" onclick="app.workspace.openLinkText('📊 대시보드/📊 전체 대시보드', '', false)">
        <div class="action-title">📊 전체 대시보드</div>
        <div class="action-desc">폴더별 진행률</div>
    </div>
    <div class="action-card action-card-2" onclick="app.commands.executeCommandById('smart-lecture-tracker:create-lecture-note')">
        <div class="action-title">🎬 새 강의 추가</div>
        <div class="action-desc">강의 노트 생성</div>
    </div>
    <div class="action-card action-card-3" onclick="app.workspace.openLinkText('🎯 PARA 메인 대시보드', '', false)">
        <div class="action-title">🎯 PARA 시스템</div>
        <div class="action-desc">전체 시스템</div>
    </div>
    <div class="action-card action-card-4" onclick="app.workspace.openLinkText('강의시리즈', '', false)">
        <div class="action-title">📁 강의 시리즈</div>
        <div class="action-desc">모든 강의 탐색</div>
    </div>
</div>
`;

dv.paragraph(actionsHTML);
```

---

## 🎯 시각적 학습 카드

```dataviewjs
const seriesPath = "강의시리즈";
const allPages = dv.pages('"' + seriesPath + '"');

const seriesGroups = {};
for (const page of allPages) {
    const pathParts = page.file.path.split('/');
    if (pathParts.length >= 2) {
        const folderName = pathParts[1];
        if (!seriesGroups[folderName]) {
            seriesGroups[folderName] = { lectures: [], completedLectures: 0, totalStudyTime: 0 };
        }
        
        if (page.file.name !== folderName && page['course-number']) {
            const tasks = page.file.tasks || [];
            const lectureTasks = tasks.filter(t => t.text.includes("#강의학습"));
            const total = lectureTasks.length;
            const done = lectureTasks.filter(t => t.completed).length;
            const individualProgress = total > 0 ? Math.round((done / total) * 100) : 0;
            
            const isCompleted = individualProgress === 100;
            
            seriesGroups[folderName].lectures.push({
                page: page,
                individualProgress: individualProgress,
                isCompleted: isCompleted
            });
            
            if (isCompleted) seriesGroups[folderName].completedLectures++;
            seriesGroups[folderName].totalStudyTime += page["total-minutes"] || 0;
        }
    }
}

const validSeries = Object.entries(seriesGroups).filter(([name, data]) => data.lectures.length > 0);

if (validSeries.length === 0) {
    dv.paragraph("📝 아직 등록된 강의 시리즈가 없습니다.");
} else {
    for (const [seriesName, seriesData] of validSeries) {
        const seriesProgress = seriesData.lectures.length > 0 ? 
            Math.round(seriesData.lectures.reduce((sum, lecture) => sum + lecture.individualProgress, 0) / seriesData.lectures.length) : 0;
        const completionRate = Math.round((seriesData.completedLectures / seriesData.lectures.length) * 100);
        
        let seriesEmoji = "📚";
        if (seriesName.includes('영어')) seriesEmoji = "🇬🇧";
        else if (seriesName.includes('수학')) seriesEmoji = "🔢";
        else if (seriesName.includes('과학')) seriesEmoji = "🧪";
        
        const hours = Math.floor(seriesData.totalStudyTime / 60);
        const minutes = seriesData.totalStudyTime % 60;
        const timeDisplay = seriesData.totalStudyTime > 0 ? 
            (hours > 0 ? `${hours}h ${minutes}m` : `${minutes}m`) : "0m";
        
        let statusEmoji = "🟡";
        if (completionRate === 100) statusEmoji = "✅";
        else if (seriesProgress >= 80) statusEmoji = "🔥";
        else if (seriesProgress === 0) statusEmoji = "⭕";
        
        dv.paragraph(`### ${seriesEmoji} **${seriesName}** ${statusEmoji}`);
        dv.paragraph(`**진행률**: ${seriesProgress}% | **완료율**: ${completionRate}% | **학습시간**: ${timeDisplay}`);
        
        const progressBar = '🟩'.repeat(Math.floor(seriesProgress/10)) + '⬜'.repeat(10 - Math.floor(seriesProgress/10));
        dv.paragraph(`${progressBar} ${seriesProgress}%`);
        
        dv.paragraph(`📊 **시리즈 대시보드**: [[강의시리즈/${seriesName}/${seriesName}|🔗 ${seriesName} 상세보기]]`);
        dv.paragraph(`---`);
    }
    
    let totalLectures = 0;
    let totalCompleted = 0;
    let grandTotalTime = 0;
    
    for (const [seriesName, seriesData] of validSeries) {
        totalLectures += seriesData.lectures.length;
        totalCompleted += seriesData.completedLectures;
        grandTotalTime += seriesData.totalStudyTime;
    }
    
    const overallProgress = totalLectures > 0 ? Math.round((totalCompleted / totalLectures) * 100) : 0;
    const totalHours = Math.floor(grandTotalTime / 60);
    const totalMinutes = grandTotalTime % 60;
    
    dv.paragraph(`## 🏆 전체 통계`);
    dv.paragraph(`**전체 완료율**: ${overallProgress}% (${totalCompleted}/${totalLectures}강)`);
    dv.paragraph(`**전체 학습 시간**: ${totalHours}시간 ${totalMinutes}분`);
    dv.paragraph(`**활성 시리즈**: ${validSeries.length}개`);
}
```

---

## 🎯 빠른 네비게이션

### 📚 **주요 링크**
- 📊 [[📊 대시보드/📊 전체 대시보드|전체 대시보드]]
- 📊 [[📊 대시보드/메인 대시보드|메인 대시보드]]
- 📁 [[강의시리즈|강의 시리즈 폴더]]
- 📄 [[3-Resources/Templates/강의 템플릿|강의 템플릿]]

---

*🚀 슈퍼허브 대시보드에서 효율적인 학습 관리를 시작하세요!*
