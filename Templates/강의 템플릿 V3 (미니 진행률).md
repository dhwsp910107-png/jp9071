---
course-number: 
lecture-name: ""
completion-date: ""
completion-time: ""
target-date: ""
total-hours: 0
total-minutes: 0
total-seconds: 0
difficulty: 3
satisfaction: 0
understanding: 0
recommend: 0
feedback: ""
tags:
  - 강의학습
  - 시리즈명
cssclasses:
  - lecture-card
---

# 🎓 {{course-number}}강 - {{lecture-name}}

> 📚 **시리즈**: 시리즈명 | 📅 **생성**: {{date}} {{time}}

---

## 🎯 미니 진행률

```dataviewjs
// 간단한 한 줄 진행률 표시
const currentFile = dv.current();
const allTasks = currentFile.file.tasks || [];
const learningTasks = allTasks.filter(t => t.text.includes("#강의학습"));
const completedTasks = learningTasks.filter(t => t.completed).length;
const totalTasks = learningTasks.length;
const progressPercent = totalTasks > 0 ? Math.round((completedTasks / totalTasks) * 100) : 0;

// 진행 아이콘
let icon = "⭕";
if (progressPercent === 100) icon = "✅";
else if (progressPercent >= 75) icon = "🔥";
else if (progressPercent >= 50) icon = "🟡";
else if (progressPercent >= 25) icon = "🔄";

// 미니 프로그레스 바
const filled = Math.floor(progressPercent / 10);
const miniBar = "🟦".repeat(filled) + "⬜".repeat(10 - filled);

dv.paragraph(`
<div style="display: flex; align-items: center; gap: 12px; padding: 12px; background: var(--background-secondary); border-radius: 8px; border-left: 4px solid #667eea;">
  <span style="font-size: 28px;">${icon}</span>
  <div style="flex: 1;">
    <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 6px;">
      <span style="font-weight: 600;">진행률</span>
      <span style="font-weight: 700; font-size: 18px; color: #667eea;">${progressPercent}%</span>
    </div>
    <div style="font-size: 13px; margin-bottom: 6px;">${miniBar}</div>
    <div style="font-size: 12px; color: var(--text-muted);">${completedTasks}/${totalTasks} 완료</div>
  </div>
</div>
`);
```

---

## ✅ 학습 체크리스트

### 🎯 기본 학습 과정
- [ ] 📖 강의 시청 완료 #강의학습 
- [ ] 📝 핵심 내용 정리 #강의학습 
- [ ] 💡 예제 따라하기 #강의학습 
- [ ] 🧠 이해도 자가평가 #강의학습 

### 🔄 복습 계획
- [ ] 🔄 1차 복습 (D+1)
- [ ] 🔄 2차 복습 (D+3)
- [ ] 🔄 3차 복습 (D+7)

---

## 📊 상세 학습 대시보드

```dataviewjs
const currentFile = dv.current();
const allTasks = currentFile.file.tasks || [];
const learningTasks = allTasks.filter(t => t.text.includes("#강의학습"));
const completedTasks = learningTasks.filter(t => t.completed).length;
const totalTasks = learningTasks.length;
const progressPercent = totalTasks > 0 ? Math.round((completedTasks / totalTasks) * 100) : 0;

// 상태 결정
let statusEmoji = "⭕";
let statusText = "시작 전";
let statusColor = "#9ca3af";

if (progressPercent === 100) {
    statusEmoji = "✅";
    statusText = "완료";
    statusColor = "#10b981";
} else if (progressPercent >= 75) {
    statusEmoji = "🔥";
    statusText = "거의 완료";
    statusColor = "#f59e0b";
} else if (progressPercent >= 50) {
    statusEmoji = "🟡";
    statusText = "진행 중";
    statusColor = "#eab308";
} else if (progressPercent >= 25) {
    statusEmoji = "🔄";
    statusText = "초반";
    statusColor = "#3b82f6";
}

// 학습 시간
const hours = currentFile["total-hours"] || 0;
const minutes = currentFile["total-minutes"] || 0;
const seconds = currentFile["total-seconds"] || 0;
const totalMinutes = hours * 60 + minutes;

// HTML 생성
dv.paragraph(`
<div style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); border-radius: 16px; padding: 24px; color: white; box-shadow: 0 4px 20px rgba(102, 126, 234, 0.3); margin: 20px 0;">
  <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 16px;">
    <div>
      <div style="font-size: 14px; opacity: 0.9; margin-bottom: 4px;">학습 진행률</div>
      <div style="font-size: 48px; font-weight: 700; line-height: 1;">${progressPercent}%</div>
    </div>
    <div style="text-align: right;">
      <div style="font-size: 36px; margin-bottom: 4px;">${statusEmoji}</div>
      <div style="font-size: 14px; opacity: 0.9;">${statusText}</div>
    </div>
  </div>
  
  <div style="background: rgba(255,255,255,0.2); height: 12px; border-radius: 999px; overflow: hidden; margin: 16px 0;">
    <div style="background: white; height: 100%; width: ${progressPercent}%; border-radius: 999px; transition: width 0.8s cubic-bezier(0.4, 0, 0.2, 1); box-shadow: 0 0 10px rgba(255,255,255,0.5);"></div>
  </div>
  
  <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 16px; margin-top: 16px;">
    <div style="text-align: center;">
      <div style="font-size: 24px; font-weight: 700;">${completedTasks}/${totalTasks}</div>
      <div style="font-size: 12px; opacity: 0.8;">완료 태스크</div>
    </div>
    <div style="text-align: center;">
      <div style="font-size: 24px; font-weight: 700;">${hours}h ${minutes}m</div>
      <div style="font-size: 12px; opacity: 0.8;">학습 시간</div>
    </div>
    <div style="text-align: center;">
      <div style="font-size: 24px; font-weight: 700;">${currentFile.difficulty}/5</div>
      <div style="font-size: 12px; opacity: 0.8;">난이도</div>
    </div>
  </div>
</div>
`);

// 완료 축하 메시지
if (progressPercent === 100 && !currentFile["completion-date"]) {
    dv.paragraph(`
<div style="background: linear-gradient(135deg, #10b981 0%, #059669 100%); border-radius: 12px; padding: 20px; color: white; text-align: center; margin-top: 16px; box-shadow: 0 4px 20px rgba(16, 185, 129, 0.3);">
  <div style="font-size: 32px; margin-bottom: 8px;">🎉</div>
  <div style="font-size: 18px; font-weight: 600; margin-bottom: 8px;">강의 완료!</div>
  <div style="font-size: 14px; opacity: 0.9;">frontmatter에 completion-date와 completion-time을 입력하세요</div>
</div>
    `);
}
```

---

## ⏱️ 학습 시간 기록

```dataviewjs
const currentFile = dv.current();
const hours = currentFile["total-hours"] || 0;
const minutes = currentFile["total-minutes"] || 0;
const seconds = currentFile["total-seconds"] || 0;
const totalMinutes = hours * 60 + minutes;

let timeColor = "#3b82f6";
let timeEmoji = "⏱️";
if (totalMinutes >= 60) {
    timeColor = "#10b981";
    timeEmoji = "🏆";
} else if (totalMinutes >= 30) {
    timeColor = "#f59e0b";
    timeEmoji = "🔥";
}

dv.paragraph(`
<div style="background: ${timeColor}; border-radius: 12px; padding: 20px; color: white; text-align: center; box-shadow: 0 2px 12px rgba(0,0,0,0.1);">
  <div style="font-size: 24px; margin-bottom: 8px;">${timeEmoji}</div>
  <div style="font-size: 14px; opacity: 0.9; margin-bottom: 4px;">총 학습 시간</div>
  <div style="font-size: 36px; font-weight: 700;">${hours}h ${minutes}m ${seconds}s</div>
</div>
`);

dv.paragraph(`
> 💡 **Study Dashboard 스톱워치 사용법**
> - 시작: \`Ctrl+P\` → "Start stopwatch"
> - 중지: \`Ctrl+P\` → "Stop stopwatch"
> - 시간이 자동으로 frontmatter에 저장됩니다
`);
```

---

## 📈 평가 및 피드백

### ⭐ 학습 평가 (0-100%)

```dataviewjs
const currentFile = dv.current();

const ratings = [
    { label: "이해도", value: currentFile.understanding || 0, emoji: "🧠", color: "#3b82f6" },
    { label: "만족도", value: currentFile.satisfaction || 0, emoji: "😊", color: "#10b981" },
    { label: "추천도", value: currentFile.recommend || 0, emoji: "👍", color: "#f59e0b" }
];

for (const rating of ratings) {
    const percentage = rating.value;
    const barWidth = Math.min(Math.max(percentage, 0), 100);
    
    let ratingLevel = "낮음";
    let ratingColor = "#ef4444";
    if (percentage >= 80) {
        ratingLevel = "최고";
        ratingColor = "#10b981";
    } else if (percentage >= 60) {
        ratingLevel = "좋음";
        ratingColor = "#3b82f6";
    } else if (percentage >= 40) {
        ratingLevel = "보통";
        ratingColor = "#f59e0b";
    }
    
    dv.paragraph(`
<div style="margin: 16px 0;">
  <div style="display: flex; justify-content: space-between; align-items: center; margin-bottom: 8px;">
    <div style="display: flex; align-items: center; gap: 8px;">
      <span style="font-size: 20px;">${rating.emoji}</span>
      <span style="font-weight: 600;">${rating.label}</span>
    </div>
    <div style="display: flex; align-items: center; gap: 12px;">
      <span style="font-size: 12px; padding: 4px 12px; background: ${ratingColor}; color: white; border-radius: 999px;">${ratingLevel}</span>
      <span style="font-size: 24px; font-weight: 700; color: ${rating.color};">${percentage}%</span>
    </div>
  </div>
  <div style="background: #e5e7eb; height: 8px; border-radius: 999px; overflow: hidden;">
    <div style="background: linear-gradient(90deg, ${rating.color}, ${ratingColor}); height: 100%; width: ${barWidth}%; border-radius: 999px; transition: width 0.5s ease;"></div>
  </div>
</div>
    `);
}

dv.paragraph(`
<div style="background: #f3f4f6; border-radius: 8px; padding: 12px; margin-top: 16px; border-left: 4px solid #667eea;">
  <div style="font-size: 12px; opacity: 0.7; margin-bottom: 4px;">💡 평가 입력 방법</div>
  <div style="font-size: 14px;">frontmatter에서 <code>understanding</code>, <code>satisfaction</code>, <code>recommend</code> 값을 0-100 사이로 입력하세요</div>
</div>
`);
```

### 🎯 난이도 평가 (1-5)

**현재 난이도**: `= this.difficulty` / 5

- ⭐ = 매우 쉬움
- ⭐⭐ = 쉬움
- ⭐⭐⭐ = 보통
- ⭐⭐⭐⭐ = 어려움
- ⭐⭐⭐⭐⭐ = 매우 어려움

> frontmatter의 `difficulty` 값을 1-5로 수정하세요

---

## 💭 학습 피드백

```dataviewjs
const currentFile = dv.current();
const feedback = currentFile.feedback || "";

dv.paragraph(`
<div style="background: linear-gradient(135deg, #fef3c7 0%, #fde68a 100%); border-radius: 12px; padding: 20px; border-left: 4px solid #f59e0b;">
  <div style="display: flex; align-items: center; gap: 8px; margin-bottom: 12px;">
    <span style="font-size: 24px;">💭</span>
    <span style="font-size: 18px; font-weight: 600; color: #92400e;">한 줄 피드백</span>
  </div>
  <div style="background: white; border-radius: 8px; padding: 16px; min-height: 60px; color: #1f2937;">
    ${feedback || '<i style="opacity: 0.5;">frontmatter의 feedback 필드에 간단한 피드백을 입력하세요</i>'}
  </div>
</div>
`);
```

### 📝 상세 학습 회고

<div class="feedback-section">

#### 🎯 잘 이해한 부분
- 
- 

#### 🤔 어려웠던 부분
- 
- 

#### 💡 핵심 인사이트
- 
- 

#### 🔗 연관 학습 자료
- 
- 

</div>

---

## 📚 학습 내용

### 🎯 핵심 개념

<div class="lecture-card">

**이 강의의 핵심 주제**:
- 
- 
- 

</div>

### 💡 중요 포인트

1. **첫 번째 포인트**
   - 설명

2. **두 번째 포인트**
   - 설명

3. **세 번째 포인트**
   - 설명

### 🔍 예제 및 실습

```
예제 코드나 내용을 여기에 작성
```

**실습 결과**:
- 

---

## 🔄 복습 타임라인

<div class="timeline">

### 1차 복습 (D+1)
**예정일**: `= date(this.completion-date) + dur(1 day)`
- [ ] 복습 완료

**복습 내용**:
- 

---

### 2차 복습 (D+3)
**예정일**: `= date(this.completion-date) + dur(3 days)`
- [ ] 복습 완료

**복습 내용**:
- 

---

### 3차 복습 (D+7)
**예정일**: `= date(this.completion-date) + dur(7 days)`
- [ ] 복습 완료

**복습 내용**:
- 

</div>

---

## 🎯 다음 단계

### 📖 다음 강의
[[{{next-course-number}}강 - ]]

### 🔗 관련 강의
- 
- 

### 📌 추가 학습 자료
- 
- 

---

## 📊 완료 상태

```dataviewjs
const currentFile = dv.current();
const completionDate = currentFile["completion-date"];
const completionTime = currentFile["completion-time"];

if (completionDate) {
    dv.paragraph(`
<div style="background: linear-gradient(135deg, #10b981 0%, #059669 100%); border-radius: 12px; padding: 20px; color: white; text-align: center; box-shadow: 0 4px 20px rgba(16, 185, 129, 0.3);">
  <div style="font-size: 32px; margin-bottom: 8px;">✅</div>
  <div style="font-size: 18px; font-weight: 600; margin-bottom: 8px;">강의 완료!</div>
  <div style="font-size: 16px; opacity: 0.9;">${completionDate} ${completionTime || ""}</div>
</div>
    `);
} else {
    const allTasks = currentFile.file.tasks || [];
    const learningTasks = allTasks.filter(t => t.text.includes("#강의학습"));
    const completedTasks = learningTasks.filter(t => t.completed).length;
    const totalTasks = learningTasks.length;
    const remaining = totalTasks - completedTasks;
    
    if (remaining > 0) {
        dv.paragraph(`
<div style="background: #f3f4f6; border-radius: 12px; padding: 20px; text-align: center; border: 2px dashed #9ca3af;">
  <div style="font-size: 32px; margin-bottom: 8px;">🎯</div>
  <div style="font-size: 16px; font-weight: 600; margin-bottom: 8px; color: #1f2937;">학습 진행 중</div>
  <div style="font-size: 14px; color: #6b7280;">남은 태스크: ${remaining}개</div>
</div>
        `);
    }
}
```

---

<div style="text-align: center; padding: 20px; opacity: 0.6; font-size: 12px;">
📚 강의: {{course-number}}강 | 📅 생성: {{date}} {{time}} | 🔄 마지막 수정: <code>= this.file.mtime</code>
</div>
