# 대시보드 재구성 적용 가이드

## 📋 적용 위치

**파일**: `.obsidian/plugins/quiz-sp2/main.js`

## 🔧 수정 방법

### 1단계: 기존 코드 찾기
- `main.js` 파일을 열고 **2222번 줄** 근처를 찾으세요
- 아래 코드가 시작되는 부분을 찾으세요:
```javascript
// 빠른 작업
const actionsSection = contentEl.createDiv({ cls: 'actions-section' });
actionsSection.createEl('h2', { text: '🚀 빠른 작업' });
```

### 2단계: 삭제할 코드 범위
아래 부분을 **모두 삭제**하세요 (약 50줄):
- `const actionsSection = contentEl.createDiv...` 부터
- `actions.forEach(action => { ... });` 까지
- (대략 2222줄 ~ 2266줄)

### 3단계: 새 코드 붙여넣기
`scripts/dashboard-reorganize-fixed.js` 파일의 내용을 **모두 복사**해서 삭제한 위치에 붙여넣으세요.

### 4단계: CSS 추가
1. `main.js`에서 `addDashboardStyles()` 메서드를 찾으세요 (약 1700번 줄 근처)
2. `style.textContent = ` 다음에 나오는 백틱(`) 안에 `scripts/dashboard-styles.css` 내용을 추가하세요

## ✅ 확인사항

적용 후 다음을 확인하세요:
1. 대시보드가 4개 섹션으로 나뉘어 있는지
   - 🎯 퀴즈 시작 (3개 버튼)
   - 📊 난이도별 퀴즈 (9개 버튼)
   - 📚 문제 관리 (6개 버튼)
   - 📈 통계 & 설정 (4개 버튼)

2. 퀴즈 시작 버튼이 보라색 그라데이션인지
3. 난이도별 버튼이 초록→빨강 색상 테두리를 가지는지
4. 모든 버튼이 정상 작동하는지

## 🚨 주의사항

- 파일 수정 전 **반드시 백업**을 만드세요
- Obsidian을 다시 시작해야 변경사항이 적용됩니다
- Emoji가 깨지면 직접 입력해서 수정하세요

## 📍 설정 페이지 경로 변경

설정 버튼의 경로는 다음과 같이 변경되었습니다:
```javascript
{ icon: '⚙️', text: '설정', callback: () => { 
    this.close(); 
    this.app.setting.open(); 
    this.app.setting.openTabById('hanzi-quiz'); 
}}
```

이 경로는 `display() { const { containerEl } = this; containerEl.empty(); containerEl.createEl('h1', { text: '⚙️ 한자 퀴즈 설정' });` 로 시작하는 설정 페이지를 엽니다.
