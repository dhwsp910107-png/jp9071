---
course-number: 
lecture-name: ""
completion-date: ""
completion-time: ""
target-date: ""
total-minutes: 0
total-hours: 0
total-seconds: 0
difficulty: 3
satisfaction: 0
understanding: 0
recommend: 0
tags:
  - 강의학습
  - 시리즈명
---

# {{course-number}}강 - {{lecture-name}}

> **시리즈**: 시리즈명 | **생성일**: {{date}} {{time}}

---

## 🎯 미니 진행률

```dataviewjs
const currentFile = dv.current();
const allTasks = currentFile.file.tasks || [];
const learningTasks = allTasks.filter(t => t.text.includes("#강의학습"));
const completedTasks = learningTasks.filter(t => t.completed).length;
const totalTasks = learningTasks.length;
const progressPercent = totalTasks > 0 ? Math.round((completedTasks / totalTasks) * 100) : 0;

let statusEmoji = "⭕";
let statusColor = "#9ca3af";
if (progressPercent === 100) { statusEmoji = "✅"; statusColor = "#10b981"; }
else if (progressPercent >= 75) { statusEmoji = "🔥"; statusColor = "#f59e0b"; }
else if (progressPercent >= 50) { statusEmoji = "🟡"; statusColor = "#eab308"; }
else if (progressPercent >= 25) { statusEmoji = "🔄"; statusColor = "#3b82f6"; }

dv.paragraph(`
<div style="background: ${statusColor}; border-radius: 8px; padding: 12px; color: white; display: flex; align-items: center; gap: 12px;">
  <div style="font-size: 32px;">${statusEmoji}</div>
  <div style="flex: 1;">
    <div style="font-size: 20px; font-weight: 700;">${progressPercent}%</div>
    <div style="font-size: 12px; opacity: 0.9;">${completedTasks}/${totalTasks} 완료</div>
  </div>
  <div style="background: rgba(255,255,255,0.3); height: 8px; flex: 2; border-radius: 999px; overflow: hidden;">
    <div style="background: white; height: 100%; width: ${progressPercent}%; border-radius: 999px; transition: width 0.5s ease;"></div>
  </div>
</div>
`);
```

---

## 📊 실시간 진행률 대시보드

```dataviewjs
const currentFile = dv.current();
const allTasks = currentFile.file.tasks || [];
const learningTasks = allTasks.filter(t => t.text.includes("#강의학습"));
const completedLearningTasks = learningTasks.filter(t => t.completed);

const totalTasks = learningTasks.length;
const completedTasks = completedLearningTasks.length;
const progressPercent = totalTasks > 0 ? Math.round((completedTasks / totalTasks) * 100) : 0;

// 진행 상태 이모지
let statusEmoji = "⭕";
if (progressPercent === 100) statusEmoji = "✅";
else if (progressPercent >= 75) statusEmoji = "🔥";
else if (progressPercent >= 50) statusEmoji = "🟡";
else if (progressPercent >= 25) statusEmoji = "🔄";

dv.paragraph(`
<div style="background: linear-gradient(135deg, #667eea 0%, #764ba2 100%); padding: 20px; border-radius: 12px; color: white; margin-bottom: 20px;">
  <h3 style="margin: 0 0 15px 0;">${statusEmoji} 학습 진행 현황</h3>
  <div style="font-size: 48px; font-weight: bold; margin: 10px 0;">${progressPercent}%</div>
  <div style="font-size: 18px; margin: 10px 0;">${completedTasks} / ${totalTasks} 완료</div>
  <div style="background: rgba(255,255,255,0.3); height: 12px; border-radius: 6px; overflow: hidden; margin-top: 15px;">
    <div style="background: white; height: 100%; width: ${progressPercent}%; transition: width 0.5s ease;"></div>
  </div>
</div>
`);

// 복습 카운트
const reviewTasks = allTasks.filter(t => t.text.includes("차 복습"));
const completedReviews = reviewTasks.filter(t => t.completed).length;

if (completedReviews > 0) {
    dv.paragraph(`🔄 **복습 완료**: ${completedReviews}회`);
}
```

---

## ⏱️ 학습 시간 트래커

```dataviewjs
const currentFile = dv.current();
const hours = currentFile["total-hours"] || 0;
const minutes = currentFile["total-minutes"] || 0;
const seconds = currentFile["total-seconds"] || 0;

const totalMinutes = hours * 60 + minutes + Math.floor(seconds / 60);
const remainingSeconds = seconds % 60;

let timeColor = "#3b82f6";
if (totalMinutes >= 60) timeColor = "#10b981";
else if (totalMinutes >= 30) timeColor = "#f59e0b";

dv.paragraph(`
<div style="background: ${timeColor}; padding: 15px; border-radius: 10px; color: white; text-align: center;">
  <div style="font-size: 14px; opacity: 0.9; margin-bottom: 5px;">⏱️ 총 학습 시간</div>
  <div style="font-size: 32px; font-weight: bold;">${hours}h ${minutes}m ${remainingSeconds}s</div>
</div>
`);

dv.paragraph(`
> 💡 **Tip**: Study Dashboard 플러그인의 스톱워치를 사용하세요!
> - 스톱워치 시작: \`Ctrl+P\` → "Start stopwatch"
> - 시간이 자동으로 기록되고, 중지하면 체크리스트가 완료됩니다.
`);
```

---

## ✅ 학습 체크리스트

### 🎯 기본 학습
- [ ] 📖 강의 시청 완료 #강의학습 
- [ ] 📝 핵심 내용 정리 완료 #강의학습 
- [ ] 💡 예제 풀이 완료 #강의학습 
- [ ] 🧠 이해도 평가 완료 #강의학습 

### 🔄 복습 사이클
- [ ] 🔄 1차 복습 (학습 후 1일)
- [ ] 🔄 2차 복습 (학습 후 3일)
- [ ] 🔄 3차 복습 (학습 후 7일)

---

## 📊 학습 평가

### 📈 진행률
- **자동 계산**: 위의 대시보드에서 실시간 확인
- 체크리스트를 완료하면 자동으로 진행률이 업데이트됩니다!

### ⭐ 난이도 (1-5)
**현재**: {{difficulty}}/5
- 1 = ⭐ 매우 쉬움
- 2 = ⭐⭐ 쉬움
- 3 = ⭐⭐⭐ 보통
- 4 = ⭐⭐⭐⭐ 어려움
- 5 = ⭐⭐⭐⭐⭐ 매우 어려움

### 💯 이해도 (0-100%)
**현재**: {{understanding}}%
> frontmatter의 `understanding` 값을 0-100 사이로 입력하세요

### ⭐ 만족도 (0-100%)
**현재**: {{satisfaction}}%

### 👍 추천도 (0-100%)
**현재**: {{recommend}}%

---

## 🎯 학습 내용

### 📌 핵심 개념

- 

### 💡 중요 포인트

- 

### 🤔 어려웠던 부분

- 

---

## 📝 학습 노트

### 오늘 배운 것



### 실습 내용



### 추가 학습 자료

- 

---

## 🔄 복습 노트

### 1차 복습 (D+1)
**날짜**: 
- [ ] 복습 완료

**복습 내용**:


### 2차 복습 (D+3)
**날짜**: 
- [ ] 복습 완료

**복습 내용**:


### 3차 복습 (D+7)
**날짜**: 
- [ ] 복습 완료

**복습 내용**:


---

## 🎯 다음 강의

[[{{next-lecture-number}}강 - ]]

---

## 📌 완료 체크

```dataviewjs
const currentFile = dv.current();
const allTasks = currentFile.file.tasks || [];
const learningTasks = allTasks.filter(t => t.text.includes("#강의학습"));
const completedLearningTasks = learningTasks.filter(t => t.completed);

const isAllComplete = learningTasks.length > 0 && completedLearningTasks.length === learningTasks.length;

if (isAllComplete && !currentFile["completion-date"]) {
    dv.paragraph(`
<div style="background: #10b981; padding: 20px; border-radius: 10px; color: white; text-align: center;">
  <h3 style="margin: 0;">🎉 강의 완료!</h3>
  <p style="margin: 10px 0 0 0;">frontmatter의 completion-date와 completion-time을 입력하세요!</p>
</div>
    `);
} else if (currentFile["completion-date"]) {
    dv.paragraph(`
<div style="background: #3b82f6; padding: 15px; border-radius: 10px; color: white; text-align: center;">
  ✅ 완료일: ${currentFile["completion-date"]} ${currentFile["completion-time"] || ""}
</div>
    `);
}
```

---

*📚 강의: {{course-number}}강 | 📅 생성: {{date}} {{time}}*
