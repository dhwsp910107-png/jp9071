---
lecture-name: "{{title}}"
instructor: "{{instructor}}"
lecture-date: "{{date}}"
course-number: {{course_number}}
total-minutes: {{total_minutes}}
segments: {{segments}}
lecture-series: "{{series}}"
completed-segments:
status: 준비중
difficulty: 
satisfaction: ""
understanding: ""
recommend: ""
study-start-time: ""
study-end-time: ""
actual-time: ""
concentration: ""
completion-date: ""
target-completion-date: "{{date+7d:YYYY-MM-DD}}"
target-review-rounds: 5
max-review-rounds: 10
review-target-date: "{{date+30d:YYYY-MM-DD}}"
tags:
  - 강의학습
  - "{{series}}"
  - 진도관리
category: 온라인강의
created: "{{date}}"
obsidian-note-status:
target date: "{{date+7d:YYYY-MM-DDTHH:mm:ss}}"
---

# 🎬 {{course_number}}강 - {{title}}

## 📋 기본 정보
- **시리즈**: [[{{series}}/{{series}}|📚 {{series}}]] ({{course_number}}강)
- **강의명**: {{title}}
- **강사**: {{instructor}}
- **학습 날짜**: {{date}}
- **강의 시간**: {{total_minutes}}분

---

## 📊 실시간 진행률 & 카운트다운

### ⏰ 학습 일정 & 마감일

```dataviewjs
// 날짜 설정
const targetCompletionDate = dv.current()["target-completion-date"];
const actualCompletionDate = dv.current()["completion-date"];
const reviewTargetDate = dv.current()["review-target-date"];
const now = new Date();

// 날짜 차이 계산 함수
function getDaysDiff(date1, date2) {
    return Math.ceil((date2 - date1) / (1000 * 60 * 60 * 24));
}

// 학습 완료 일정 체크
if (actualCompletionDate) {
    // 이미 완료된 경우
    const target = new Date(targetCompletionDate);
    const actual = new Date(actualCompletionDate);
    const daysDiff = getDaysDiff(target, actual);
    const status = daysDiff < 0 ? "조기" : daysDiff > 0 ? "지연" : "정시";
    
    dv.paragraph(`### 🎉 학습 완료!`);
    dv.paragraph(`**목표 완료일**: ${targetCompletionDate}`);
    dv.paragraph(`**실제 완료일**: ${actualCompletionDate} (${status} ${Math.abs(daysDiff)}일)`);
    
    // 복습 일정 체크
    if (reviewTargetDate) {
        const reviewTarget = new Date(reviewTargetDate);
        const reviewRemaining = getDaysDiff(now, reviewTarget);
        
        if (reviewRemaining > 0) {
            dv.paragraph(`### 🔄 복습 목표일정`);
            dv.paragraph(`**복습 목표일**: ${reviewTargetDate} (D-${reviewRemaining})`);
            
            // 복습 카운트다운 위젯
            dv.paragraph(``);
            dv.paragraph(`<div style="transform: scale(0.6); transform-origin: left top; width: 167%; margin-bottom: -30px;">`);
            dv.paragraph(`\`\`\`widgets`);
            dv.paragraph(`type: countdown`);
            dv.paragraph(`date: ${reviewTargetDate} 23:59:59`);
            dv.paragraph(`to: 🔄 ${dv.current()["lecture-name"]} 복습 완료`);
            dv.paragraph(`\`\`\``);
            dv.paragraph(`</div>`);
        } else {
            dv.paragraph(`### 🔄 복습 기간 종료`);
            dv.paragraph(`**복습 목표일**: ${reviewTargetDate} (종료됨)`);
        }
    }
} else if (targetCompletionDate) {
    // 아직 완료되지 않은 경우
    const target = new Date(targetCompletionDate);
    const remaining = getDaysDiff(now, target);
    
    if (remaining > 0) {
        dv.paragraph(`### ⏰ 목표 완료일까지`);
        dv.paragraph(`**D-${remaining}일** (${targetCompletionDate}까지)`);
        
        // 학습 카운트다운 위젯
        dv.paragraph(``);
        dv.paragraph(`<div style="transform: scale(0.6); transform-origin: left top; width: 167%; margin-bottom: -30px;">`);
        dv.paragraph(`\`\`\`widgets`);
        dv.paragraph(`type: countdown`);
        dv.paragraph(`date: ${targetCompletionDate} 23:59:59`);
        dv.paragraph(`to: 🎯 ${dv.current()["lecture-name"]} 학습 완료`);
        dv.paragraph(`\`\`\``);
        dv.paragraph(`</div>`);
    } else {
        const overdue = Math.abs(remaining);
        dv.paragraph(`### ⚠️ 목표일 ${overdue}일 초과`);
        dv.paragraph(`**목표 완료일**: ${targetCompletionDate}`);
        dv.paragraph(`**지연**: ${overdue}일`);
    }
}
```

---

```dataviewjs
// 현재 강의의 학습 진행률 자동 계산
const tasks = dv.current().file.tasks;
const lectureCompletionTasks = tasks.filter(t => t.text.includes("학습 완료") && t.text.includes("#강의학습"));
const total = lectureCompletionTasks.length;
const done = lectureCompletionTasks.filter(t => t.completed).length;
const percentage = total > 0 ? Math.round((done / total) * 100) : 0;

// 복습 진행률 계산 (무제한 차수 지원)
const content = await dv.io.load(dv.current().file.path);
const targetRounds = dv.current()["target-review-rounds"] || 5;
const maxPossibleRounds = 20; // 최대 20차까지 지원

// 모든 차수의 복습 매치 찾기
const reviewRounds = [];
for (let i = 1; i <= maxPossibleRounds; i++) {
    const pattern = new RegExp(`- \\[x\\] ${i}차 복습 🔄`, 'g');
    const matches = content.match(pattern) || [];
    if (matches.length > 0 || i <= targetRounds) {
        reviewRounds.push({
            round: i,
            completed: matches.length,
            total: total,
            progress: total > 0 ? Math.round((matches.length / total) * 100) : 0
        });
    }
}

// 완료된 전체 복습 라운드 계산 (연속된 라운드만)
let completedFullRounds = 0;
for (const round of reviewRounds) {
    if (round.progress === 100) {
        completedFullRounds++;
    } else {
        break;
    }
}

// 전체 복습 진행률 계산
const totalPossibleReviews = reviewRounds.length * total;
const totalCompletedReviews = reviewRounds.reduce((sum, round) => sum + round.completed, 0);
const overallReviewProgress = totalPossibleReviews > 0 ? Math.round((totalCompletedReviews / totalPossibleReviews) * 100) : 0;

// 현재 진행해야 할 차수 찾기
let currentRound = 1;
for (let i = 0; i < reviewRounds.length; i++) {
    if (reviewRounds[i].progress < 100) {
        currentRound = reviewRounds[i].round;
        break;
    }
    if (i === reviewRounds.length - 1 && reviewRounds[i].progress === 100) {
        currentRound = reviewRounds[i].round + 1;
    }
}

// 진행바 생성
const progressBar = "▓".repeat(Math.floor(percentage/10)) + "░".repeat(10-Math.floor(percentage/10));
const reviewProgressBar = "🔄".repeat(Math.floor(overallReviewProgress/10)) + "░".repeat(10-Math.floor(overallReviewProgress/10));

// 결과 출력
dv.paragraph(`### 🎯 강의 진행 현황`);
dv.paragraph(`**총 ${total}개 구간 중 ${done}개 완료 (${percentage}%)**`);
dv.paragraph(`**진행바**: ${progressBar}`);
dv.paragraph(`<progress value="${done}" max="${total}" style="width: 100%; height: 25px; background: linear-gradient(90deg, #6366f1, #8b5cf6);"></progress>`);

dv.paragraph(`### 🔄 복습 현황`);
dv.paragraph(`**완료된 전체 라운드**: ${completedFullRounds}회 연속 완료`);
dv.paragraph(`**전체 복습 진행률**: ${overallReviewProgress}%`);
dv.paragraph(`**복습 진행바**: ${reviewProgressBar}`);
dv.paragraph(`<progress value="${overallReviewProgress}" max="100" style="width: 100%; height: 20px; background: linear-gradient(90deg, #f59e0b, #ef4444);"></progress>`);

if (percentage === 100) {
    if (currentRound > targetRounds) {
        dv.paragraph(`### 🏆 완전 마스터!`);
        dv.paragraph(`**축하합니다! 모든 학습과 복습을 완료했습니다!**`);
    } else {
        dv.paragraph(`### 🎉 강의 완료! 복습 진행중`);
        dv.paragraph(`**다음 복습**: ${currentRound}차 복습 진행 필요`);
    }
} else if (percentage > 0) {
    dv.paragraph(`### 📚 학습 진행중`);
    dv.paragraph(`**현재 ${percentage}% 진행 중입니다. 계속 화이팅!**`);
}

// 복습 상세 현황
if (reviewRounds.length > 0) {
    dv.paragraph(`### 📊 복습 상세 현황`);
    const reviewTable = reviewRounds.map(round => {
        const status = round.progress === 100 ? "✅ 완료" : 
                      round.progress > 0 ? `🔄 ${round.progress}%` : "⭕ 미시작";
        const progressBar = "▓".repeat(Math.floor(round.progress/10)) + "░".repeat(10-Math.floor(round.progress/10));
        return [
            `${round.round}차`,
            `${round.completed}/${round.total}`,
            `${progressBar} ${round.progress}%`,
            status
        ];
    });
    
    dv.table(
        ["차수", "완료/전체", "진행률", "상태"],
        reviewTable
    );
}
```

---

## ⏱️ 10분 단위 세부 진행

<%* 
// 세그먼트 개수에 따라 동적으로 구간 생성
const segments = tp.frontmatter["segments"] || 6;
const totalMinutes = tp.frontmatter["total-minutes"] || 60;
const segmentDuration = Math.round(totalMinutes / segments);

for (let i = 1; i <= segments; i++) {
    const startTime = (i - 1) * segmentDuration;
    const endTime = i * segmentDuration;
_%>

### 📍 {{i}}구간 ({{startTime}}-{{endTime}}분)
- [ ] **학습 완료** ✅ #강의학습  [completion:: ]
- **주요 내용**:
  ```
  {{이 구간에서 학습한 핵심 내용을 정리하세요}}
  ```
- **메모**:
  ```
  {{추가 메모나 중요 포인트}}
  ```
- **복습 체크**: 
  - [ ] 1차 복습 🔄 [completion:: ]
  - [ ] 2차 복습 🔄
  - [ ] 3차 복습 🔄
  - [ ] 4차 복습 🔄
  - [ ] 5차 복습 🔄
- **복습 필요**: [ ] (어려운 부분이면 체크)

---

<%* } _%>

## 📝 전체 정리

### 🎯 핵심 요약
```
{{강의 전체의 핵심 내용을 요약하세요}}
```

### 💡 새로 배운 내용
```
{{이번 강의에서 새롭게 알게 된 내용}}
```

### ❓ 질문/의문점
```
{{학습 중 생긴 질문이나 의문점을 기록하세요}}
```

### 🔄 복습 필요 사항
```
{{다시 복습이 필요한 부분이나 어려웠던 내용을 기록하세요}}
```

---

## 📈 학습 현황

### ⏱️ 시간 기록
- **시작 시간**: `$= this["study-start-time"] || "미기록"`
- **완료 시간**: `$= this["study-end-time"] || "미기록"`
- **실제 소요 시간**: `$= this["actual-time"] || "미기록"`
- **집중도**: `$= (this.concentration || "미평가") + " (5점 만점)"`

### 🎯 전체 평가
- **내용 난이도**: `$= (this.difficulty ? "⭐".repeat(this.difficulty) + " " + this.difficulty : "미평가") + "/5"`
- **강의 만족도**: `$= (this.satisfaction ? "⭐".repeat(this.satisfaction) + " " + this.satisfaction : "미평가") + "/5"`
- **전체 이해도**: `$= (this.understanding ? "⭐".repeat(this.understanding) + " " + this.understanding : "미평가") + "/5"`
- **추천 여부**: `$= this.recommend || "미평가"`

---

## 🏷️ 메타데이터 & 수정 정보
- **생성일**: `$= this.created`
- **마지막 수정**: `$= dv.current().file.mtime.toFormat("yyyy-MM-dd HH:mm")`
- **시리즈**: [[{{series}}/{{series}}|📚 {{series}}]]
- **강의 번호**: `$= this["course-number"] + "강"`
- **상태**: `$= this.status`

### ⏰ 일정 관리
- **목표 완료일**: `$= this["target-completion-date"] || "미설정"`
- **실제 완료일**: `$= this["completion-date"] || "미완료"`
- **복습 목표일**: `$= this["review-target-date"] || "미설정"`

---

## ⚡ 빠른 링크 & 대시보드

```button
name 🏠 시리즈 대시보드
type link
action [[{{series}}/{{series}}|🎯 {{series}} 전체 현황]]
```
^button-series-dashboard

```button
name 📊 전체 대시보드
type link
action [[1-Projects/강의학습시스템/📊 대시보드/📊 폴더강의 전체 대시보드|📊 강의학습 진행률 대시보드]]
```
^button-main-dashboard

```button
name 🎬 새 강의 추가
type link
action [[3-Resources/Templates/새 강의 추가 마법사|🎬 새 강의 추가]]
```
^button-new-lecture

```button
name 🔄 진행률 새로고침
type command
action Dataview: Force Refresh All Views
```
^button-refresh

---